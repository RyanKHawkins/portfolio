import projects from "projects.json"

console.log(projects);
let projectArray = JSON.parse(projects);

// function sortProjects() {
//     projects = projects.sort((a, b) => a.name - b.name)
// }
console.log(projectArray);